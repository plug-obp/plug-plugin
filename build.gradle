
group 'fr.ensta-bretagne'
version '0.2'

apply plugin: 'groovy'
apply plugin: 'maven-publish'

repositories {
    maven {
        url "https://plugins.gradle.org/m2/"
    }
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
}


publishing {
    repositories {
        maven { url "/Volumes/data/webperso/mocs-artefacts/plug-repo" }
    }
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
